// Placeholder entry point.
//
// The design deliverable is THERMOX.dc.html at the repository root -- a single
// self-contained file that opens directly in a browser. Port it into this Vite
// app when the React/TypeScript build is wanted; the API contract it speaks is
// documented in docs/API.md and is already what the FastAPI backend serves.
document.getElementById("root").textContent =
  "THERMOX frontend shell. See THERMOX.dc.html for the design implementation.";
